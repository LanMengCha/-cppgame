#include <string>
#include <vector>
#include <cctype>
#include <iostream>
#ifdef WIDTH
#define MAX_WIDTH = 150
#define S_WIDTH = 50
#define MAX_HIGH = 60
#define S_HIGH = 20
explicit unsigned int num_p = 0;
//最长\短的宽和高
#endif